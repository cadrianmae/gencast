name: REPLACE_ME
description: ONE_LINE_DESCRIPTION

# TTS backend. openai (cloud, paid) or speaches (local, free).
tts_provider: openai
tts_model: tts-1-hd

speakers:
  - name: REPLACE_ME_HOST1
    voice_id: nova    # openai voices: alloy, echo, fable, onyx, nova, shimmer
    backstory: |
      REPLACE_ME — short paragraph on who this character is.
    personality: |
      REPLACE_ME — how they talk, their quirks, what they do/don't lean on.
    avatar:
      emoji: "🦊"
      color: "#ff7847"

  - name: REPLACE_ME_HOST2
    voice_id: echo
    backstory: |
      REPLACE_ME
    personality: |
      REPLACE_ME
    avatar:
      emoji: "🐻"
      color: "#4a90e2"
